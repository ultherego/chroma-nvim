{
  "contract": 1,
  "id": "terraform",
  "name": "Terraform / OpenTofu",
  "description": "Plan, review and apply from the editor, and formatting.",
  "requires": ["core"],
  "tools": {
    "required": [
      { "any": ["terraform", "tofu"], "reason": "the runner and the formatter both shell out to one of them" }
    ],
    "recommended": [
      { "id": "terragrunt", "reason": "directories holding terragrunt.hcl are run and formatted with it" }
    ]
  },
  "nvim": {
    "modules": ["chroma-terraform"],
    "plugins": ["plugins.lsp", "plugins.formatting", "plugins.lint"]
  }
}
