{
  "contract": 5,
  "id": "helm",
  "name": "Helm",
  "description": "Chart templates: filetype detection, language server, rendering.",
  "requires": [
    "core"
  ],
  "tools": {
    "required": [
      {
        "id": "helm",
        "reason": "rendering and linting a chart, both of which work without a cluster"
      }
    ]
  },
  "nvim": {
    "servers": [
      "helm_ls"
    ],
    "plugins": [
      "vim-helm"
    ],
    "parsers": [
      "helm",
      "gotmpl"
    ]
  }
}
