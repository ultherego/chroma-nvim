{
  "contract": 4,
  "id": "kubernetes",
  "name": "Kubernetes",
  "description": "Cluster views, contexts, namespaces, logs.",
  "requires": [
    "core"
  ],
  "tools": {
    "required": [
      {
        "id": "kubectl",
        "reason": "every cluster view, and the context and namespace pickers"
      }
    ]
  },
  "nvim": {
    "plugins": [
      "kubectl.nvim"
    ],
    "schemas": [
      "kubernetes"
    ]
  }
}
