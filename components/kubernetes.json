{
  "contract": 1,
  "id": "kubernetes",
  "name": "Kubernetes",
  "description": "Cluster views, contexts, namespaces, logs.",
  "requires": ["core"],
  "tools": {
    "required": [
      { "id": "kubectl", "reason": "every cluster view, and the context and namespace pickers" }
    ]
  },
  "nvim": {
    "plugins": ["plugins.devops"]
  }
}
