{
  "contract": 1,
  "id": "aws",
  "name": "AWS",
  "description": "Profile and region for this session, and identity-bound Terraform plans.",
  "requires": ["core"],
  "tools": {
    "required": [
      { "id": "aws", "reason": "listing profiles and regions, and sts get-caller-identity" }
    ]
  },
  "nvim": {
    "modules": ["chroma-aws"]
  }
}
