{
  "contract": 2,
  "id": "ansible",
  "name": "Ansible",
  "description": "Playbooks and roles: running them, and the language server.",
  "requires": [
    "core"
  ],
  "tools": {
    "required": [
      {
        "id": "ansible",
        "reason": "running a playbook or a role from the buffer"
      }
    ]
  },
  "nvim": {
    "plugins": [
      "plugins.devops",
      "plugins.lint"
    ]
  }
}
