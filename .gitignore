# lazy.nvim (lazy-lock.json is tracked on purpose — reproducible plugin set)
lazy-lock.json.bak

# runtime / state
.luarc.json
*.log
spell/
shada/
sessions/

# editor / os
.DS_Store
*.swp
*~
.direnv/
